import styled from 'styled-components';
import Input from '../../inputs/Input';

const LoginValueInput = styled(Input)`
  width: 100%;
  height: 48px;
  font-size: 13px;
`;

export default LoginValueInput;
