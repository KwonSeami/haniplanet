import * as React from 'react';
import Page403 from '../../components/errors/Page403';

const SeminarNewMobile = React.memo(() => (
  <Page403 message="세미나/모임모집 개설은 PC에서 이용 가능합니다." />
));

export default SeminarNewMobile;
